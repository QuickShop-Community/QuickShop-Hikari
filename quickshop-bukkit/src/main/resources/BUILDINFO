git.tags=${git.tags}
git.branch=${git.branch}
git.dirty=${git.dirty}
git.remote.origin.url=${git.remote.origin.url}
git.commit.id=${git.commit.id}
git.commit.id.abbrev=${git.commit.id.abbrev}
git.commit.id.describe=${git.commit.id.describe}
git.commit.id.describe-short=${git.commit.id.describe-short}
git.commit.user.name=${git.commit.user.name}
git.commit.user.email=${git.commit.user.email}
git.commit.message.short=${git.commit.message.short}
git.commit.time=${git.commit.time}
git.build.user.name=${git.build.user.name}
git.build.user.email=${git.build.user.email}
git.build.time=${git.build.time}
git.build.host=${git.build.host}
git.build.version=${git.build.version}
git.build.number=${git.build.number}

ci.is=${env.CI}
ci.build.number=${env.BUILD_NUMBER}
ci.build.id=${env.BUILD_ID}
ci.build.name=${env.BUILD_DISPLAY_NAME}
ci.build.tag=${env.BUILD_TAG}
ci.build.url=${env.BUILD_URL}
ci.job.name=${env.JOB_NAME}
ci.job.base_name=${env.JOB_BASE_NAME}
ci.job.url=${env.JOB_URL}
ci.job.display_url=${env.JOB_DISPLAY_URL}
ci.executor.number=${env.EXECUTOR_NUMBER}
ci.node.name=${env.NODE_NAME}
ci.node.labels=${env.NODE_LABELS}
ci.home=${env.JENKINS_HOME}
ci.url=${env.JENKINS_URL}
