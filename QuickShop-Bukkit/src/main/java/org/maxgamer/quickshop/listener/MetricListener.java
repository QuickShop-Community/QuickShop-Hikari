package org.maxgamer.quickshop.listener;

import org.maxgamer.quickshop.QuickShop;

public class MetricListener extends AbstractQSListener {
    public MetricListener(QuickShop plugin) {
        super(plugin);
    }
}
